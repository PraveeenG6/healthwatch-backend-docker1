[build]
builder = "RAILPACK"

[deploy]
startCommand = "java -jar target/healthmonitor-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/ping"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
